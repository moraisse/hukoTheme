<div class="wrap" style="max-width:820px;padding-top:32px;padding-bottom:88px">
  <div class="eyebrow" style="margin-bottom:18px">{{ article.published_at | date: '%d.%m.%Y' }}</div>
  <h1 class="serif" style="font-size:46px;margin:0 0 28px">{{ article.title }}</h1>
  <div class="rte">{{ article.content }}</div>
</div>