<div style="text-align:center;padding:40px">
  <img src="{{ 'huko-logo.svg' | asset_url }}" alt="{{ shop.name }}" style="height:60px;margin:0 auto 30px">
  <h1 class="serif" style="font-size:38px">Em breve</h1>
  <p class="rte" style="margin:0 auto">{{ shop.password_message }}</p>
</div>