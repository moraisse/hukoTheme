<section class="section" data-screen-label="Página">
  <div class="wrap" style="max-width:760px">
    <div class="sec-head" style="flex-direction:column;align-items:flex-start"><div><h2>{{ page.title }}</h2></div></div>
    <div class="rte" style="font-size:18px;color:var(--muted);line-height:1.7">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Página" }
{% endschema %}
